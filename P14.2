/*14.2 swap two number by using function 
(call the referance)*/
#include <stdio.h>

void main() {
int a =10;
int b =25;
printf("before swap value of a=%d and b=%d\n",a,b);
swap(&a,&b);
printf("after swapping value of a=%d and b=%d\n",a,b);
}
    void swap (int *x, int *y)
{
    
 int temp;
 temp = *x;
 *x = *y;
 *y = temp;
}

out put

before swap value of a=10 and b=25
after swapping value of a=25 and b=10
